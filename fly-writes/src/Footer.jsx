const Footer = () => (
  <footer className="flex items-center justify-center">
    <p className="text-white opacity-50 text-xs" >&copy; Fly Engineering — {new Date().getFullYear()}</p>
  </footer>
);

export default Footer; // Make sure to have this line
